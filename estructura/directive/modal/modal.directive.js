app.directive("modal", function($window, config, global) {

	return {
	    restrict: 'A',
	    templateUrl: 'directive/modal/modal.template.html',
	    scope: {},
	  	link: function(scope, element) {
	  		
	  		// code

	  		// Se inicializa el juego
	  		scope.clean = function() {
	  			
	  			// code
	  		}
	  	}
  	};	  
  
});